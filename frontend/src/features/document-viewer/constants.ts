/** Mirrors backend `EMBEDDING_MODEL_NAME` until exposed via document detail API. */
export const DEFAULT_EMBEDDING_MODEL = 'sentence-transformers/all-MiniLM-L6-v2'

// Zoom bounds/step live in `./utils/zoomMath` — the zoom gesture pipeline owns them.

export const VIEWER_DRAWER_TRANSITION_MS = 200
