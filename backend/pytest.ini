[pytest]
testpaths =
    tests/unit
    tests/integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (fast, no external services).
    integration: API integration tests (TestClient; DB checks are patched in conftest).
addopts =
    -ra
    --strict-markers
